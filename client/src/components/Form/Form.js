import React from 'react'
import useStyles from './Styles';

const Form = () => {
    const classes = useStyles();
    return (
        <div>
            FORM
        </div>
    )
}

export default Form;
